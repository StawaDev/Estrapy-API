def errors():
    "An Function Error That Will Be Used For Handle Error From API Server Down"
    global api_down
    api_down = (
        "We might have a problem with the API, Please wait for it to be available"
    )
